const HTML_CLASS = {
    tblName: "tbl-name"
};

const HTML_ID = {
    chooseTblBtn: "choose-tbl-btn",
    searchTblInput: "search-tbl-input",
    tblNameDropdown: "tbl-name-dropdown"
};

const HTML_TAG = {
    a: "a"
};

const HTML_PROPERTY = {
    id: "id"
};

const CSS_PROPERTY = {
    display: "display"
};

const CSS_VALUE = {
    none: "none",
    block: "block"
}

const EVENT = {
    click: "click",
    submit: "submit",
    keyup: "keyup"
};

export {
    HTML_CLASS,
    HTML_ID,
    HTML_TAG,
    HTML_PROPERTY,
    CSS_PROPERTY,
    CSS_VALUE,
    EVENT
};