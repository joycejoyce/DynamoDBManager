const HTML_CLASS = {
    
}

const HTML_ID = {
    chooseTblBtn: "choose-tbl-btn",
    searchTblInput: "search-tbl-input"
};

const HTML_PROPERTY = {
    id: "id"
};

const CSS_PROPERTY = {
    display: "display"
};

const CSS_VALUE = {
    none: "none",
    block: "block"
}

const EVENT = {
    click: "click",
    submit: "submit"
};

export {
    HTML_CLASS,
    HTML_ID,
    HTML_PROPERTY,
    CSS_PROPERTY,
    CSS_VALUE,
    EVENT
};