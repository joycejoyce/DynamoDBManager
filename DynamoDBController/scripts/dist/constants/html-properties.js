"use strict";

System.register([], function (_export, _context) {
  "use strict";

  var HTML_CLASS, HTML_ID, HTML_TAG, HTML_PROPERTY, CSS_PROPERTY, CSS_VALUE, EVENT;
  return {
    setters: [],
    execute: function () {
      _export("HTML_CLASS", HTML_CLASS = {
        tblName: "tbl-name"
      });

      _export("HTML_ID", HTML_ID = {
        chooseTblBtn: "choose-tbl-btn",
        searchTblInput: "search-tbl-input",
        tblNameDropdown: "tbl-name-dropdown"
      });

      _export("HTML_TAG", HTML_TAG = {
        a: "a"
      });

      _export("HTML_PROPERTY", HTML_PROPERTY = {
        id: "id"
      });

      _export("CSS_PROPERTY", CSS_PROPERTY = {
        display: "display"
      });

      _export("CSS_VALUE", CSS_VALUE = {
        none: "none",
        block: "block"
      });

      _export("EVENT", EVENT = {
        click: "click",
        submit: "submit",
        keyup: "keyup"
      });
    }
  };
});